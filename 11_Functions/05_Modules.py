# There are teo typed of modules :
# 1. Built-in modules
# 2. External modules (User defined modules)

# Built-in modules are those that come with Python, such as `math`, `os`, and `sys`.

import math 

print(math.lcm(7,9))
print(math.log(100,10))


# We can also create our own modules. A module is simply a Python file that contains functions, classes, or variables that we want to use in other Python files.
# To create a module, we simply create a Python file with a `.py` extension and define our functions or classes in it.
# For example, we can create a file named `my_module.py` with the following content


# We can install Many Modules by Using pip 

# pip install module_name

import requests

a = requests.get('https://www.udemy.com/course/codewithharry-python/learn/lecture/48756787#reviews')


